name: 01 - Building Blocks
on: push
    workflow_dispatch

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps: 
      - name: say Hello
        run: echo "Hello, world!"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps: 
      - name: Failed Step
        run: |
          echo "I will fail"
          exit 1
      - name: Say Goodbye
        run: echo "Goodbye!"
